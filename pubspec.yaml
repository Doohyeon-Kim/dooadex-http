name: dooadex_http
description: A useful http package.
version: 0.1.2
repository: "https://github.com/Doohyeon-Kim/dooadex-http.git"

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  dooadex_error_handler: ^0.1.1
  dooadex_logger: ^0.1.1
  dooadex_constants: ^0.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter: